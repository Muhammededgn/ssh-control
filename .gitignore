/target

# Local agent guidance, not shared with the repo
/CLAUDE.md

# makepkg build artifacts — regenerated by `makepkg`, never source
/packaging/arch/pkg/
/packaging/arch/src/
/packaging/arch/*.pkg.tar.zst
/packaging/arch/*.log
